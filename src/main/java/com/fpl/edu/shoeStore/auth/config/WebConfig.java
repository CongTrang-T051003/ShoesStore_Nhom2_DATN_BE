package com.fpl.edu.shoeStore.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Cấu hình liên quan đến web (CORS, Interceptors, etc.)
}
